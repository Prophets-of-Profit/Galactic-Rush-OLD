package com.prophetsofprofit.galacticrush.graphics.player

import com.badlogic.gdx.graphics.Color

/**
 * All the possible colors the player can choose
 * TODO: handle different drone sprites, etc.
 */
enum class PlayerColor(color: Color) {
    BLUE(Color.BLUE),
    CYAN(Color.CYAN),
    GREEN(Color.GREEN),
    PINK(Color.PINK),
    PURPLE(Color.PURPLE),
    RED(Color.RED),
    YELLOW(Color.YELLOW)
}
